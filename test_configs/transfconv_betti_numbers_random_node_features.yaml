task: betti_numbers
model:
  model_name: TransfConv
  hidden_channels: 64
  num_hidden_layers: 3
  num_node_features: 8
  out_channels: 3
trainer:
  accelerator: auto
  max_epochs: 50
  log_every_n_steps: 1
litmodel:
  learning_rate: 0.01
data:
  transforms: random_node_features
  use_stratified: false
  seed: 1234
